import { TheatresSortOrderPipe } from './theatres-sort-order.pipe';

describe('TheatresSortOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new TheatresSortOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
